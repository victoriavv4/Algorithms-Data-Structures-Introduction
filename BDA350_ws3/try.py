x = [1, 2, 6, 7, None]
i = 4
print(max(len(x) / 2, i))

# to check and see if the non-None values (i) are larger than halving the length of the capacity
# this will return the larger of the 2 values. Halving the length of the capacity, or the number of nonNone values








